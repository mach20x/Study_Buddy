<%= form_with(model: classroom, local: true) do |form| %>
  <% if classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user, id: :classroom_user %>
  </div>

  <div class="field">
    <%= form.label :school %>
    <%= form.text_field :school, id: :classroom_school %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
